// @flow
export type Props = {
  addable?: boolean,
  id?: number,
  image?: string,
  name?: string,
  email?: string,
  color?: string,
  slug: string,
  onClick?: Function,
  removeClient: Function,
}
